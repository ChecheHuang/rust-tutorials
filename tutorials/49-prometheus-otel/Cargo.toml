[package]
name = "ch49-prometheus-otel"
edition.workspace = true
version.workspace = true

[[bin]]
name = "ch49"
path = "src/main.rs"

[dependencies]
tokio = { workspace = true }
axum = "0.7"
anyhow = { workspace = true }
metrics = "0.23"
metrics-exporter-prometheus = "0.15"
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
# OpenTelemetry 部分 API 漂移頻繁，這個範例先聚焦在 metrics + tracing fmt。
# 接 OTLP exporter 的範例見 README，因 OTel SDK 版本變動快，請對齊當下版本。
